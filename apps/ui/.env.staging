# Vite environment variables for staging
# Used when building for staging environment

VITE_APP_VERSION=0.1.0-staging

# API endpoints
VITE_BFF_BASE_URL=https://staging-api.niyati.example.com
VITE_N8N_WEBHOOK_URL=https://staging-webhook.niyati.example.com/webhook/chat

# Feature flags
VITE_DEBUG_MODE=false
VITE_VERBOSE_LOGGING=false
