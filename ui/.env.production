# Vite environment variables for production
# Used when building for production deployment

VITE_APP_VERSION=0.1.0

# API endpoints (relative URLs for same-origin)
VITE_BFF_BASE_URL=
VITE_N8N_WEBHOOK_URL=https://nonexperientially-nonascetical-agnes.ngrok-free.dev/webhook/chat

# Feature flags
VITE_DEBUG_MODE=false
VITE_VERBOSE_LOGGING=false
