{
  "name": "@niyati/auth-core",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "description": "Reusable authentication utilities — OAuth PKCE, rate limiting, email, JWT middleware, validation helpers",
  "scripts": {
    "test": "jest --config jest.config.cjs --runInBand"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "express-rate-limit": "^7.1.5",
    "jsonwebtoken": "^9.0.0",
    "node-cache": "^5.1.2",
    "bcryptjs": "^2.4.3",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
